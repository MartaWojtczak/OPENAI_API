HOW TO CREATE VIRTUAL ENV TO CONNECT SPOTIFY
commands in the same place where .env file

1. python -m venv env
2. env\Scripts\activate

now you are in virtual environment 

3. install packages (pip install ...)

4. run your script :)